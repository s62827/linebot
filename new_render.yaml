services:
  - type: web
    name: flask-expense-tracker
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: RENDER_APP_URL
        value: https://flask-expense-tracker.onrender.com
